services:
  nginx:
    container_name: nginx
    restart: always
    build: .
    ports:
      - "80:80"
    volumes:
      - ./html/poradnik:/var/www/poradnik
    networks:
      - net

networks:
  net:
    external: true